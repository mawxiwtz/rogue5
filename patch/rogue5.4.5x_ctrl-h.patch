diff --git a/command.c b/command.c
index f058879..1a94063 100644
--- a/command.c
+++ b/command.c
@@ -219,6 +219,7 @@ command(void)
 		last_pick = NULL;
 	    }
 over:
+	    if (ch == 0x7f) ch = CTRL('H');
 	    switch (ch)
 	    {
 		case ',': {
